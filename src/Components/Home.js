import Typography from "@mui/material/Typography";

function Home() {
  return (
    <Typography
      variant="h4"
      sx={{
        color: "primary.main",
        textAlign: "center",
        paddingY: 2,
      }}
    >
      Home
    </Typography>
  );
}

export default Home;
