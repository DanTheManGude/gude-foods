import Typography from "@mui/material/Typography";

function Home() {
  return (
    <Typography
      variant="h4"
      sx={{
        color: "primary.main",
        textAlign: "center",
      }}
    >
      Home
    </Typography>
  );
}

export default Home;
